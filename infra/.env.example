# PostgreSQL (eine Instanz, zwei Datenbanken)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me

# devdesign_api – setze Passwort wie oben ein
DEVDESIGN_DATABASE_URL=postgresql://postgres:change_me@postgres:5432/devdesign_db

# preise_api
PREISE_DATABASE_URL=postgresql://postgres:change_me@postgres:5432/preiseberechnen_db

# Optional: Log-Level (INFO, DEBUG, …)
# LOG_LEVEL=INFO
